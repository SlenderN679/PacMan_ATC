#pragma once // Diretiva do pré-processador para garantir que o arquivo do cabeçalho seja incluído apenas uma vez durante a compilação

enum Directions { UP, DOWN, LEFT, RIGHT };// Declaração de uma enumeração chamada Directions com quatro valores possíveis: UP, DOWN, LEFT e RIGHT