#pragma once// Diretiva do pré-processador para garantir que o arquivo do cabeçalho seja incluído apenas uma vez durante a compilação


struct Size_TXY { // Estrutura que define um par de coordenadas (x, y) do tipo size_t
    size_t x, y;
};

struct IntXY { // Estrutura que define um par de coordenadas (x, y) do tipo int
    int x, y;
};